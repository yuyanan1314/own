<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fast.user.dao.SysRoleMenuDao">

	<resultMap id="BaseResultMap" type="com.fast.user.entity.SysRoleMenu">
		<id column="id" property="id" />
		<result column="role_id" property="roleId" />
		<result column="menu_id" property="menuId" />
	</resultMap>

    <!-- 通用查询结果列-->
    <sql id="Base_Column_List">
        id, role_id AS roleId, menu_id AS menuId
    </sql>
    
    <select id="listMenuIdByRoleId" resultType="String">
		select menu_id from sys_role_menu
		<where>role_id = #{roleId}</where>
	</select>

	<delete id="removeByRoleId">
		DELETE FROM sys_role_menu WHERE role_id=#{roleId}
	</delete>

	<insert id="batchSave">
		INSERT INTO sys_role_menu(id,role_id, menu_id) values
		<foreach item="item" index="index" collection="list"
			separator=",">
			(#{item.id},#{item.roleId},#{item.menuId})
		</foreach>
	</insert>
</mapper>
